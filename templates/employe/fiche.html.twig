<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; font-size: 14px; }
        h2 { color: #2c3e50; }
        .section { margin-bottom: 10px; }
        .label { font-weight: bold; }
    </style>
</head>
<body>
    <h2>Fiche Employé</h2>

    <div class="section">
        <span class="label">Nom:</span> {{ employe.nom }}
    </div>
    <div class="section">
        <span class="label">Email:</span> {{ employe.email }}
    </div>
    <div class="section">
        <span class="label">Poste:</span> {{ employe.poste }}
    </div>
    <div class="section">
        <span class="label">Salaire:</span> {{ employe.salaire }} MAD
    </div>
    <div class="section">
        <span class="label">Date d'embauche:</span> {{ employe.dateEmbauche|date('Y-m-d') }}
    </div>
    <div class="section">
        <span class="label">Statut:</span> {{ employe.isActive ? 'Actif' : 'Inactif' }}
    </div>
</body>
</html>
